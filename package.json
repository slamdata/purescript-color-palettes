{
  "private": true,
  "scripts": {
    "build": "pulp build -- --censor-lib --strict",
    "watch": "pulp --watch build -- --censor-lib --strict",
    "ide": "purs ide server"
  },
  "dependencies": {
    "pulp": "^12.0.1",
    "purescript": "^0.11.7",
    "purescript-psa": "^0.5.1"
  }
}
